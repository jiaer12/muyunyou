// 默认参数
const DEFAULTS ={
    initialIndex:0,             // 初始索引（第0张照片）
    animation:true,             // 切换时是否有动画
    speed:300,                  // 切换速度，单位ms
    autoplay:0                  // 自动切换，单位ms（0为不开启自动切换，其他正数为开启自动切换）
};
export default DEFAULTS;